# Compatibility

Teltonika RMS supports Home Assistant 2026.7.0 or newer.

The supported minimum follows the current stable monthly Home Assistant release. The value is kept consistent across `hacs.json`, `requirements-dev.in` and this document by a blocking repository check.
